import Tabs from '../tabs/Tabs';
import styles from './General.module.css';

const General = (props) => {
  return (
    <div>
      <Tabs currentTab='general' />
      Функция не реализована
    </div>
  );
};

export default General;
